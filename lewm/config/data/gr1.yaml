dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  repo_id: "gr1_pickup_grasp"
  keys_to_load:
    - world_center
    - world_left
    - world_right
    - world_top
    - world_wrist
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
